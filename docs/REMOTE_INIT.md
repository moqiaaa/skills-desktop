# 远程初始化配置说明

## 🚀 工作原理

应用程序首次启动时会：

1. 检查 `C:\Users\用户名\.skills-manager\config.json` 是否存在
2. 如果**不存在**（首次运行），从远程下载初始化数据
3. 将数据保存到 `.skills-manager` 目录
4. 后续启动跳过下载，直接使用本地数据

## 📂 目录结构

**所有数据都在 `.skills-manager` 目录下**：

```
C:\Users\用户名\.skills-manager\
├── config.json              # 应用配置
├── security-rules.json      # 安全规则库（可选）
├── marketplace.json         # 市场数据（可选）
└── skills\                  # 已安装的技能
    ├── git-commander\
    │   └── SKILL.md
    ├── file-explorer\
    │   └── SKILL.md
    └── ...更多技能
```

**项目级技能**（如果配置了项目路径）：
```
D:\Projects\MyProject\.skills-manager\
└── skills\
    └── project-specific-skill\
        └── SKILL.md
```

## 🔧 配置下载源

### 方式 1: GitHub Release (推荐)

在 `main.rs` 中设置：
```rust
const INIT_DATA_URL: &str = "https://github.com/你的用户名/skill-manager/releases/latest/download/init-data.zip";
```

**创建 `init-data.zip`**:
```
init-data.zip
├── config.json              # 应用配置
├── security-rules.json      # 安全规则（可选）
├── marketplace.json         # 市场数据（可选）
└── skills/                  # 预装技能（可选）
    ├── example-skill/
    │   └── SKILL.md
    └── another-skill/
        └── SKILL.md
```

**发布到 GitHub Release**:
1. 在仓库创建 Release
2. 上传 `init-data.zip`
3. 用户首次启动时自动下载

### 方式 2: 直接下载 JSON

如果只需要配置文件：
```rust
const INIT_DATA_URL: &str = "https://raw.githubusercontent.com/你的用户名/skill-manager/main/config.json";
```

代码会自动识别：
- `.zip` 结尾 → 解压到目录
- 其他 → 直接保存为 config.json

### 方式 3: 自托管服务器

```rust
const INIT_DATA_URL: &str = "https://your-server.com/skill-manager/init-data.zip";
```

## 📦 init-data.zip 内容示例

```
init-data.zip
├── config.json
├── security-rules.json
├── marketplace.json
└── skills/                    # 预装技能
    ├── git-commander/
    │   ├── SKILL.md
    │   └── skill.py
    └── file-explorer/
        ├── SKILL.md
        └── skill.py
```

**config.json**:
```json
{
  "projectPaths": [],
  "defaultInstallLocation": "system",
  "defaultProjectPath": null,
  "githubRepo": "你的用户名/skill-manager",
  "updatedAt": "2026-01-06T00:00:00Z"
}
```

**注意**: ZIP 解压后，`skills/` 目录中的所有技能会自动出现在应用中！

## ⚙️ 更新下载源

编辑 `src-tauri/src/main.rs` 第 13 行：
```rust
const INIT_DATA_URL: &str = "你的下载地址";
```

然后重新编译：
```bash
npm run tauri:build:windows
```

## 🔄 用户数据管理

用户可以：
1. **查看配置**: 打开 `C:\Users\用户名\.skills-manager\`
2. **手动编辑**: 直接修改 `config.json`
3. **重置**: 删除 `.skills-manager` 目录，重启应用重新下载
4. **备份**: 复制整个 `.skills-manager` 目录

## 💡 优势

- ✅ **exe 体积小**: 不包含配置数据
- ✅ **灵活更新**: 可以在线更新配置而不需要重新打包 exe
- ✅ **用户友好**: 自动下载，无需手动配置
- ✅ **离线可用**: 下载后可离线使用
- ✅ **数据分离**: 程序和数据分离，便于管理

## ⚠️ 注意事项

1. **网络连接**: 首次启动需要网络连接
2. **失败处理**: 如果下载失败，使用默认配置继续运行
3. **下载速度**: 取决于网络和文件大小
4. **GitHub 限流**: 使用 GitHub Release 注意 API 限制

## 🎯 使用场景

这种方案特别适合：
- 配置文件需要频繁更新
- 有大量预置数据（如规则库、模板）
- 多个用户共享同一配置源
- 企业内网部署（可以用内网服务器）
